{
	"name": "showcase",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"frame-master": "3.1.1"
	},
	"scripts": {
		"dev": "bun run serve.ts",
		"serve:out": "bun run serve.ts",
		"prompt": "bun run index.ts",
		"prompt:history": "bun run index.ts --conversation=hr-showcase-default"
	}
}
